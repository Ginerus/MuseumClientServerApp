<UserControl x:Class="MeseumClient.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             mc:Ignorable="d">

    <Grid>
        <!-- Основная сетка: шапка + контент -->
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Шапка -->
        <Grid Background="#1E90FF" Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Логотип и название -->
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="10,0">
                <Ellipse Width="40" Height="40">
                    <Ellipse.Fill>
                        <ImageBrush ImageSource="pack://application:,,,/Resources/logo.png" Stretch="UniformToFill"/>
                    </Ellipse.Fill>
                </Ellipse>
                <TextBlock Text="Память времён"
                           Foreground="White"
                           FontSize="20"
                           VerticalAlignment="Center"
                           Margin="10,0,0,0"/>
            </StackPanel>

            <!-- Роль пользователя -->
            <TextBlock Text="{Binding UserRole}" 
                       Foreground="White"
                       FontSize="16"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Right"
                       Margin="0,0,10,0"
                       Grid.Column="1"/>
        </Grid>

        <!-- Контент с сайдбаром -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="220"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Sidebar -->
            <StackPanel Background="#2C2C2C">
                <Button Content="О музее" Command="{Binding ShowAboutTabCommand}" Margin="10"/>
                <Button Content="Экспонаты" Command="{Binding ShowExhibitsTabCommand}" Margin="10"/>
                <!-- Добавляй новые кнопки аналогично -->
            </StackPanel>

            <!-- Контент вкладок -->
            <Grid Grid.Column="1">

                <!-- Основной контент вкладки -->
                <ContentControl Content="{Binding CurrentTab.Content}" Margin="0,40,0,0"/>
            </Grid>
        </Grid>
    </Grid>
</UserControl>