<UserControl x:Class="MuseumClient.Views.Details.AddExhibitView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">

    <Grid Background="White">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="450"/>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- ЛЕВАЯ ЧАСТЬ -->

        <StackPanel Grid.Column="0"
                    Margin="20">

            <Border Height="500"
                    Background="White"
                    CornerRadius="16"
                    BorderBrush="#E5E7EB"
                    BorderThickness="2">

                <Grid>

                    <Image Source="{Binding Image}"
                           Stretch="Uniform"
                           Margin="10"/>

                    <TextBlock Text="Изображение не выбрано"
                               Foreground="#9CA3AF"
                               FontSize="16"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"/>

                </Grid>

            </Border>

            <Button Content="Выбрать изображение"
                    Height="40"
                    Margin="0,15,0,0"
                    Background="#4A90E2"
                    Foreground="White"
                    BorderThickness="0"
                    FontWeight="SemiBold"
                    Cursor="Hand"
                    Command="{Binding SelectImageCommand}">

                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8">

                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>

                        </Border>
                    </ControlTemplate>
                </Button.Template>

            </Button>

            <TextBlock Text="{Binding SelectedImagePath}"
                       Margin="0,10,0,0"
                       TextWrapping="Wrap"
                       Foreground="#6B7280"
                       FontSize="12"/>

        </StackPanel>

        <!-- ПРАВАЯ ЧАСТЬ -->

        <ScrollViewer Grid.Column="2"
                      VerticalScrollBarVisibility="Auto">

            <StackPanel Margin="20">

                <!-- HEADER -->

                <Border Background="White"
                        CornerRadius="16"
                        Padding="15"
                        Margin="0,0,0,15"
                        BorderBrush="#E5E7EB"
                        BorderThickness="2">

                    <Grid>

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel>

                            <TextBlock Text="Создание экспоната"
                                       FontSize="24"
                                       FontWeight="Bold"
                                       Foreground="#1F2937"/>

                            <TextBlock Text="Заполните информацию о новом экспонате"
                                       Margin="0,4,0,0"
                                       FontSize="13"
                                       Foreground="#6B7280"/>

                        </StackPanel>

                        <Button Grid.Column="1"
                                Content="Создать"
                                Width="140"
                                Height="36"
                                Background="#4A90E2"
                                Foreground="White"
                                BorderThickness="0"
                                FontSize="14"
                                FontWeight="SemiBold"
                                Cursor="Hand"
                                Command="{Binding SaveCommand}">

                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            CornerRadius="8">

                                        <ContentPresenter HorizontalAlignment="Center"
                                                          VerticalAlignment="Center"/>

                                    </Border>
                                </ControlTemplate>
                            </Button.Template>

                        </Button>

                    </Grid>

                </Border>

                <!-- НАЗВАНИЕ -->

                <Border Background="White"
                        BorderBrush="#E5E7EB"
                        BorderThickness="2"
                        CornerRadius="12"
                        Padding="15"
                        Margin="0,0,0,15">

                    <StackPanel>

                        <TextBlock Text="Название"
                                   FontSize="16"
                                   FontWeight="SemiBold"
                                   Foreground="#374151"/>

                        <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
                                 Margin="0,8,0,0"
                                 Height="35"/>

                    </StackPanel>

                </Border>

                <!-- ХАРАКТЕРИСТИКИ -->

                <UniformGrid Columns="2"
                             Margin="0,0,0,15">

                    <!-- Материалы -->

                    <Border Margin="0,0,10,0"
                            Background="White"
                            BorderBrush="#E5E7EB"
                            BorderThickness="2"
                            CornerRadius="12"
                            Padding="15">

                        <StackPanel>

                            <TextBlock Text="Материалы"
                                       FontSize="16"
                                       FontWeight="SemiBold"
                                       Foreground="#374151"/>

                            <TextBox Text="{Binding Materials, UpdateSourceTrigger=PropertyChanged}"
                                     Margin="0,8,0,0"/>

                        </StackPanel>

                    </Border>

                    <!-- Хранение -->

                    <Border Background="White"
                            BorderBrush="#E5E7EB"
                            BorderThickness="2"
                            CornerRadius="12"
                            Padding="15">

                        <StackPanel>

                            <TextBlock Text="Тип хранения"
                                       FontSize="16"
                                       FontWeight="SemiBold"
                                       Foreground="#374151"/>

                            <CheckBox Content="Постоянное хранение"
                                      Margin="0,10,0,0"
                                      IsChecked="{Binding IsPermanent}"/>

                        </StackPanel>

                    </Border>

                </UniformGrid>

                <!-- ОТДЕЛ -->

                <Border Background="White"
                        BorderBrush="#E5E7EB"
                        BorderThickness="2"
                        CornerRadius="12"
                        Padding="15"
                        Margin="0,0,0,15">

                    <StackPanel>

                        <TextBlock Text="Отдел"
                                   FontSize="16"
                                   FontWeight="SemiBold"
                                   Foreground="#374151"/>

                        <ComboBox Margin="0,8,0,0"
                                  Height="35"
                                  ItemsSource="{Binding Departments}"
                                  SelectedItem="{Binding SelectedDepartment}"
                                  DisplayMemberPath="Name"/>

                    </StackPanel>

                </Border>

                <!-- ОПИСАНИЕ -->

                <Border Background="White"
                        BorderBrush="#E5E7EB"
                        BorderThickness="2"
                        CornerRadius="12"
                        Padding="15">

                    <StackPanel>

                        <TextBlock Text="Описание"
                                   FontSize="18"
                                   FontWeight="SemiBold"
                                   Foreground="#1F2937"/>

                        <Separator Margin="0,10"/>

                        <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                                 AcceptsReturn="True"
                                 TextWrapping="Wrap"
                                 VerticalScrollBarVisibility="Auto"
                                 Height="250"/>

                    </StackPanel>

                </Border>

            </StackPanel>

        </ScrollViewer>

    </Grid>

</UserControl>